-- tiny-inline-diagnostic.nvim was removed in favor of Neovim's built-in
-- `virtual_lines` diagnostic handler. See `lua/plugins/lspconfig.lua`'s
-- `opts.diagnostics` for the replacement configuration (including the custom
-- `format` that preserves the plugin's `show_source` capability).
return {}
